# AGENTS.md

## Cursor Cloud specific instructions

This repository (`EUROPA`) is currently an empty/greenfield project containing only a `README.md`. There are no applications, services, dependencies, build systems, or tests to run.

- **No package manager or lockfile** exists yet — the update script is a no-op (`echo`).
- **No lint, test, or build commands** are available.
- **No services** need to be started.

Once application code is added, this section should be updated with instructions for running, testing, and developing the project.
